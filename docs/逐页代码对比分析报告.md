# 逐页代码对比分析报告

**项目 A**: `C:\Users\13609\Projects\crm-ui-upgrade`
**项目 B**: `C:\Users\13609\.openclaw\workspace\crm2026-4-3new`

**生成时间**: 2026-04-09

---

## 概述

本报告对两个 CRM 项目的页面代码进行了逐页、逐文件、逐代码块的详细对比分析。

### 项目规模对比

| 指标 | 项目 A | 项目 B | 差异 |
|------|--------|--------|------|
| 页面文件总数 | 119 个 | 95 个 | 项目 A 多 24 个 |
| 核心业务页面 | 完整 | 部分简化 | - |
| AI 功能页面 | 15 个 | 9 个 | 项目 A 更完整 |
| 报表页面 | 12 个 | 5 个 | 项目 A 更完整 |
| 设置页面 | 24 个 | 18 个 | 项目 A 更完整 |

### 架构差异总结

| 维度 | 项目 A | 项目 B |
|------|--------|--------|
| UI 框架 | shadcn/ui + Tailwind | Ant Design + 自研组件 |
| 表格组件 | DataTable (@tanstack/react-table) | DataTable (Ant Design) |
| 表单组件 | 自研 Form + shadcn | Ant Design Form |
| 状态管理 | React Hooks + TanStack Query | React Hooks + Mock Data |
| 路由 | React Router v6 | React Router v6 |
| 国际化 | react-i18next | react-i18next |

---

## 页面文件总览表

### 项目 A 独有页面（24 个）

```
src/pages/ai/AIAnalytics.tsx
src/pages/ai/AIAssistant.tsx
src/pages/ai/AIConfig.tsx
src/pages/ai/AIDashboard.tsx
src/pages/ai/AIHistory.tsx
src/pages/ai/AIModels.tsx
src/pages/ai/AIPromptTemplates.tsx
src/pages/ai/AIUsage.tsx
src/pages/custom-objects/CustomObjectBuilder.tsx
src/pages/custom-objects/CustomObjectDetail.tsx
src/pages/custom-objects/CustomObjectList.tsx
src/pages/custom-objects/CustomObjectRecordForm.tsx
src/pages/custom-objects/CustomObjectSettings.tsx
src/pages/Dashboard/Charts.tsx
src/pages/Dashboard/DashboardEditor.tsx
src/pages/Dashboard/DashboardPage.tsx
src/pages/Dashboard/DashletPanel.tsx
src/pages/Dashboard/GoalProgress.tsx
src/pages/Dashboard/QuickActions.tsx
src/pages/Dashboard/RecentActivity.tsx
src/pages/Dashboard/StatsCards.tsx
src/pages/Dashboard/TeamUpdates.tsx
src/pages/Dashboard/TodoList.tsx
src/pages/reports/ReportBuilder.tsx
src/pages/reports/ReportDashboard.tsx
src/pages/reports/ReportDetail.tsx
src/pages/reports/ReportExport.tsx
src/pages/reports/ReportList.tsx
src/pages/reports/ReportSchedule.tsx
src/pages/settings/AdvancedSettings.tsx
src/pages/settings/APISettings.tsx
src/pages/settings/DataBackupSettings.tsx
src/pages/settings/EmailSettings.tsx
src/pages/settings/ImportExportSettings.tsx
src/pages/settings/IntegrationSettings.tsx
src/pages/settings/LayoutSettings.tsx
src/pages/settings/LicenseSettings.tsx
src/pages/settings/MobileSettings.tsx
src/pages/settings/NotificationSettings.tsx
src/pages/settings/PreferencesSettings.tsx
src/pages/settings/ProfileSettings.tsx
src/pages/settings/SecuritySettings.tsx
src/pages/settings/SystemInfo.tsx
src/pages/settings/ThemeSettings.tsx
src/pages/settings/WebhookSettings.tsx
src/pages/settings/WorkflowSettings.tsx
src/pages/workflows/WorkflowBuilder.tsx
src/pages/workflows/WorkflowDetail.tsx
src/pages/workflows/WorkflowExecutions.tsx
src/pages/Workbench.tsx
```

### 项目 B 独有页面（若干）

```
src/pages/callcenter/OutboundTasks.tsx
src/pages/knowledge/KnowledgeSearch.tsx
src/pages/pricebooks/PricebookDetail.tsx
src/pages/pricebooks/PricebookList.tsx
src/pages/products/ProductDetail.tsx
src/pages/products/ProductList.tsx
src/pages/settings/ChangePassword.tsx
src/pages/settings/CreateCustomObject.tsx
src/pages/settings/DisplayPreferences.tsx
src/pages/settings/ObjectConfig.tsx
src/pages/settings/ObjectData.tsx
src/pages/settings/ObjectFields.tsx
src/pages/workbench/components/AIAssistant.tsx
src/pages/workbench/components/AIRecommendation.tsx
src/pages/workbench/components/CreateModal.tsx
src/pages/workbench/components/Performance.tsx
src/pages/workbench/components/QuickActions.tsx
src/pages/workbench/components/TodayTodo.tsx
src/pages/workbench/components/WinPredictionCard.tsx
src/pages/TestPage.tsx
```

---

## 同名页面代码对比

### 1. CustomerList.tsx 对比

| 对比项 | 项目 A | 项目 B | 差异说明 |
|--------|--------|--------|----------|
| **组件结构** | 函数组件 `CustomerList` + `export default` | 函数组件 `CustomerList` + `export default` | 结构相似 |
| **UI 框架** | shadcn/ui + Tailwind CSS | Ant Design + 自研组件 | **重大差异** |
| **表格实现** | DataTable (@tanstack/react-table) | DataTable (Ant Design Table) | 不同底层库 |
| **API 调用** | TanStack Query hooks (`useCustomers`) | Mock data (`getCustomerList`) | **项目 A 有真实 API，项目 B 用 Mock** |
| **状态管理** | React.useState + useMemo | React.useState + useMemo | 相似 |
| **筛选功能** | FilterBar + 高级筛选 (FilterGroup) | FilterBar + 基础筛选 | **项目 A 支持高级组合筛选** |
| **表单字段** | name, company, email, phone, status, score, assignee | name, industry, companySize, level, source, phone, email, address, remark | **字段差异大** |
| **按钮功能** | 新建、导出、编辑、删除、批量分配 | 新建、保存筛选、列设置、导出、编辑、删除 | 项目 A 功能更完整 |
| **批量操作** | 发送邮件、分配负责人、导出、删除 | 分配、删除 | **项目 A 多 2 个批量操作** |
| **行内编辑** | 不支持 | 不支持 | 相同 |
| **数据导出** | Excel/CSV/JSON 三选一 | 未实现 (coming soon) | **项目 A 已实现** |

#### 关键代码差异

**项目 A - API 调用**:
```typescript
const { data, isLoading } = useCustomers()
const createMutation = useCreateCustomer()
const updateMutation = useUpdateCustomer()
const deleteMutation = useDeleteCustomer()
const bulkDeleteMutation = useBulkDeleteCustomers()
const exportMutation = useExportCustomers()
const bulkAssignMutation = useBulkAssignCustomers()
```

**项目 B - Mock 数据**:
```typescript
const loadCustomerList = () => {
  setLoading(true);
  try {
    const { list, total } = getCustomerList({
      ...filters,
      page,
      pageSize,
    });
    setCustomerList(list);
    setTotal(total);
  } catch (error) {
    message.error('加载客户列表失败');
  } finally {
    setLoading(false);
  }
};
```

**项目 A - 高级筛选**:
```typescript
const [advancedFilterGroup, setAdvancedFilterGroup] = React.useState<FilterGroup>(
  () => createEmptyFilterGroup('AND')
)

const filteredData = React.useMemo(() => {
  if (advancedFilterGroup.conditions.length > 0) {
    return applyFilterGroup(customers, advancedFilterGroup)
  }
  // ...简单筛选逻辑
}, [data?.data, filterValues, advancedFilterGroup])
```

**项目 B - 基础筛选**:
```typescript
const handleFilterChange = (values: Record<string, any>) => {
  setFilters(values);
  setPage(1);
};
```

---

### 2. CustomerDetail.tsx 对比

| 对比项 | 项目 A | 项目 B | 差异说明 |
|--------|--------|--------|----------|
| **布局结构** | DetailLayout 三栏布局 | 自研三栏布局 | 相似但实现不同 |
| **左侧边栏** | InfoCard + ActionButtons | 自研 Sidebar + ActionButtons | 项目 A 组件化更好 |
| **中间内容** | DetailTabs (概览/活动/联系人/商机) | Tabs (概览/活动/AI Insights) | **项目 B 有 AI 洞察** |
| **右侧边栏** | RelatedListCard (沟通记录/商机/活动) | 公司信息/商机/工单/联系人卡片 | 项目 B 信息更丰富 |
| **行内编辑** | InlineEditableField 支持所有字段 | 不支持行内编辑 | **项目 A 支持行内编辑** |
| **API 调用** | useCustomer, useUpdateCustomer, useContactsByCustomer | getCustomerById (Mock) | **项目 A 有真实 API** |
| **关联数据** | 自动加载联系人和商机 | Mock 数据 | 项目 A 更真实 |
| **AI 功能** | 无 | CustomerSummaryAI, RelationshipChangeAI, SmartSuggestionsAI, RiskAlertAI | **项目 B 独有 AI 洞察** |

#### 关键代码差异

**项目 A - 行内编辑**:
```typescript
const handleInlineEdit = async (field: keyof Customer, value: string) => {
  if (!customer) return
  setInlineEditLoading(field)
  try {
    await updateMutation.mutateAsync({
      id: customerId,
      [field]: value,
    })
    toast({ title: t("customer.inlineEditSuccess", "已更新") })
    refetch()
  } catch (error) {
    toast({ title: t("customer.inlineEditFailed", "更新失败"), variant: "destructive" })
  } finally {
    setInlineEditLoading(null)
  }
}
```

**项目 B - AI 洞察**:
```typescript
<CustomerSummaryAI customerId={id} customerName={customer.name} />
<RelationshipChangeAI customerId={id} />
<SmartSuggestionsAI customerId={id} />
<RiskAlertAI customerId={id} />
```

---

### 3. OpportunityList.tsx 对比

| 对比项 | 项目 A | 项目 B | 差异说明 |
|--------|--------|--------|----------|
| **视图模式** | 列表/漏斗/Kanban 三视图 | 列表/Kanban 两视图 | **项目 A 多漏斗视图** |
| **漏斗可视化** | 自定义 FunnelView 组件 | SalesFunnel 组件 | 实现不同 |
| **阶段配置** | 6 阶段 (初步接触/需求确认/方案报价/合同谈判/成交/失败) | 7 阶段 (英文枚举) | **阶段定义不同** |
| **API 调用** | TanStack Query hooks | Mock data (`opportunityData`) | **项目 A 有真实 API** |
| **阶段推进** | useAdvanceOpportunityStage, useCloseOpportunityWon, useCloseOpportunityLost | 无实际逻辑 (仅 message) | **项目 A 功能完整** |
| **统计卡片** | 按阶段分组统计 + 总金额/加权金额 | 总数 + 总金额 | **项目 A 统计更详细** |
| **筛选功能** | 7 个基础筛选 + 9 个高级筛选字段 | 4 个基础筛选 | **项目 A 筛选更强大** |
| **看板功能** | 外部 Kanban 页面 | 内置 KanbanBoard 组件 | 项目 B 集成更好 |

#### 关键代码差异

**项目 A - 漏斗视图**:
```typescript
function FunnelView({ opportunities, onOpportunityClick, onAdvanceStage, onCloseWon, onCloseLost }) {
  const stages: OpportunityStage[] = ["初步接触", "需求确认", "方案报价", "合同谈判", "成交"]
  const stageStats = opportunities.reduce((acc, opp) => {
    if (opp.stage !== "失败") {
      acc[opp.stage] = (acc[opp.stage] || { count: 0, amount: 0 })
      acc[opp.stage].count++
      acc[opp.stage].amount += opp.amount
    }
    return acc
  }, {} as Record<OpportunityStage, { count: number; amount: number }>)
  // ...可视化渲染
}
```

**项目 A - 阶段操作**:
```typescript
const handleAdvanceStage = React.useCallback(async (id: string) => {
  try {
    await advanceMutation.mutateAsync(id)
  } catch (error) {
    console.error("推进阶段失败:", error)
  }
}, [advanceMutation])

const handleCloseWon = async (id: string) => {
  try {
    await closeWonMutation.mutateAsync({ id })
  } catch (error) {
    console.error("成交失败:", error)
  }
}
```

**项目 B - 看板数据**:
```typescript
const kanbanColumns = useMemo((): KanbanColumn[] => {
  const activeStages = [
    OpportunityStage.LEAD_CONFIRMATION,
    OpportunityStage.INITIAL_CONTACT,
    OpportunityStage.REQUIREMENT_CONFIRMATION,
    OpportunityStage.PROPOSAL_QUOTATION,
    OpportunityStage.NEGOTIATION_APPROVAL,
  ];

  return activeStages.map(stage => ({
    key: stage,
    title: stageDisplayNames[stage],
    color: stageColors[stage],
    cards: filteredData
      .filter(opp => opp.stage === stage)
      .map(opp => ({
        id: opp.id,
        title: opp.name,
        amount: opp.amount,
        customer: opp.customerName,
        owner: opp.ownerName,
        dueDate: opp.estimatedCloseDate,
        priority: opp.amount > 500000 ? 'high' : opp.amount > 200000 ? 'medium' : 'low',
      })),
  }));
}, [filteredData]);
```

---

### 4. QuoteList.tsx / QuotesList.tsx 对比

| 对比项 | 项目 A (QuoteList.tsx) | 项目 B (QuotesList.tsx) | 差异说明 |
|--------|------------------------|------------------------|----------|
| **文件命名** | QuoteList.tsx | QuotesList.tsx | 命名规范不同 |
| **组件导出** | `QuoteListPage` | `QuotesList` | 不同 |
| **UI 框架** | shadcn/ui | Ant Design | **重大差异** |
| **数据源** | Mock data (硬编码) | Mock data (`getQuoteList`) | 项目 B 更规范 |
| **状态类型** | 'draft' | 'sent' | 'accepted' | 'rejected' | 'expired' | QuoteStatus 枚举 | **项目 B 类型更安全** |
| **筛选功能** | 状态下拉筛选 | 报价单号/客户/状态三筛选 | **项目 B 筛选更完整** |
| **操作按钮** | 发送/查看/编辑/复制/下载/删除 | 查看/编辑 (草稿)/转合同 (已接受)/删除 | **项目 B 有转合同功能** |
| **统计卡片** | 6 个 (总数/草稿/已发送/已接受/已拒绝/总报价额) | 无独立统计卡片 | **项目 A 统计更直观** |
| **PDF 生成** | 无 | 无 | 相同 (都未实现) |

#### 关键代码差异

**项目 A - 状态配置**:
```typescript
const statusColors = {
  draft: 'bg-gray-100 text-gray-700',
  sent: 'bg-blue-100 text-blue-700',
  accepted: 'bg-green-100 text-green-700',
  rejected: 'bg-red-100 text-red-700',
  expired: 'bg-orange-100 text-orange-700',
}
```

**项目 B - 转合同功能**:
```typescript
const handleConvertToContract = (id: string) => {
  Modal.confirm({
    title: t('quote.list.convertConfirm'),
    content: t('quote.list.convertContent'),
    okText: t('common.actions.confirm'),
    cancelText: t('common.actions.cancel'),
    onOk: () => {
      message.success(t('quote.list.convertSuccess'));
      loadQuoteList();
    },
  });
};
```

---

### 5. FieldSettings.tsx / CustomFields.tsx 对比

| 对比项 | 项目 A (FieldSettings.tsx) | 项目 B (CustomFields.tsx) | 差异说明 |
|--------|---------------------------|--------------------------|----------|
| **页面名称** | 字段设置 | 自定义字段 | 语义不同 |
| **UI 框架** | shadcn/ui + Table | 自研组件 + 分组卡片 | **布局差异大** |
| **字段类型** | 12 种 (text/textarea/number/date/datetime/select/multiselect/switch/user/department/relation/file) | 15 种 (新增 currency/percent/radio/checkbox/email/phone/url) | **项目 B 类型更丰富** |
| **组织方式** | 扁平列表 + 模块筛选 | 按对象分组 (customer/lead/opportunity/contact/contract/product) | **项目 B 组织更清晰** |
| **API 调用** | useFieldSettings, useCreateField, useUpdateField, useDeleteField | useCustomFields, useCreateCustomField, useUpdateCustomField, useDeleteCustomField, useBulkDeleteCustomFields, useUpdateFieldSort, useToggleFieldActive | **项目 B API 更完整** |
| **批量操作** | 无 | 批量删除 | **项目 B 支持批量操作** |
| **拖拽排序** | 有图标但无实现 | 完整拖拽排序实现 | **项目 B 已实现** |
| **字段统计** | 无 | 每个对象显示总计/启用/必填数量 | **项目 B 有统计** |
| **系统字段** | 无标识 | isSystem 标识，不可删除 | **项目 B 有保护机制** |

#### 关键代码差异

**项目 A - 字段类型**:
```typescript
const fieldTypeLabels: Record<string, { label: string; color: string }> = {
  text: { label: "文本", color: "bg-blue-100 text-blue-700" },
  textarea: { label: "多行文本", color: "bg-blue-100 text-blue-700" },
  number: { label: "数字", color: "bg-green-100 text-green-700" },
  // ...共 12 种
}
```

**项目 B - 拖拽排序**:
```typescript
const handleDragStart = useCallback((e: React.DragEvent, field: CustomField, index: number, objectName: ObjectName) => {
  e.dataTransfer.setData('fieldId', field.id);
  e.dataTransfer.setData('startIndex', index.toString());
  e.dataTransfer.setData('objectName', objectName);
}, []);

const handleDrop = useCallback((e: React.DragEvent, endIndex: number, objectName: ObjectName) => {
  const fieldId = e.dataTransfer.getData('fieldId');
  const startIndex = parseInt(e.dataTransfer.getData('startIndex'));
  
  if (startIndex !== endIndex) {
    const objectFields = groupedFields[objectName];
    const updates: Array<{ id: string; sort: number }> = [];
    // ...计算新排序
    sortMutation.mutate(updates);
  }
}, [groupedFields, sortMutation]);
```

---

### 6. PermissionManagement.tsx 对比

| 对比项 | 项目 A | 项目 B | 差异说明 |
|--------|--------|--------|----------|
| **页面完整性** | 完整实现 (权限矩阵 + 数据范围 + 字段权限) | 占位页面 (仅标题和描述) | **项目 A 功能完整** |
| **UI 复杂度** | 三栏布局 (角色列表 + Tabs) | 单卡片 | 项目 A 复杂得多 |
| **权限矩阵** | PermissionMatrix 组件，支持 CRUD 操作 | 无 | **项目 A 独有** |
| **数据范围** | 5 级范围 (本人/本部门/本部门及下级/全公司/自定义) | 无 | **项目 A 独有** |
| **字段权限** | 可见性/可编辑性开关 | 无 | **项目 A 独有** |
| **角色管理** | 内置角色列表，支持切换 | 无 | **项目 A 独有** |
| **保存机制** | 检测变更 + 批量保存 | 无 | **项目 A 独有** |
| **API 调用** | Mock 数据 (但结构完整) | 无 | 项目 A 更接近真实 |

#### 关键代码差异

**项目 A - 权限矩阵**:
```typescript
<PermissionMatrix
  matrix={permissionMatrix}
  onChange={handleMatrixChange}
  isModified={isModified}
/>
```

**项目 A - 数据范围**:
```typescript
<Select
  value={dataScope[module.id] || DataScope.SELF}
  onValueChange={(value: DataScope) =>
    handleDataScopeChange(module.id, value)
  }
>
  <SelectTrigger className="w-[200px]">
    <SelectValue />
  </SelectTrigger>
  <SelectContent>
    {DATA_SCOPE_OPTIONS.map(option => (
      <SelectItem key={option.value} value={option.value}>
        <div className="flex flex-col">
          <span>{option.label}</span>
          <span className="text-xs text-muted-foreground">{option.desc}</span>
        </div>
      </SelectItem>
    ))}
  </SelectContent>
</Select>
```

**项目 B - 占位实现**:
```typescript
const PermissionManagement: React.FC = () => {
  return (
    <div className="space-y-6">
      <div className="flex items-center gap-2">
        <Key className="w-6 h-6 text-primary" />
        <h1 className="text-2xl font-semibold">权限配置</h1>
      </div>
      
      <Card>
        <CardHeader>
          <CardTitle>系统权限配置</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-gray-500">配置系统权限策略，管理功能访问和数据权限。</p>
        </CardContent>
      </Card>
    </div>
  );
};
```

---

### 7. WorkflowList.tsx 对比

| 对比项 | 项目 A | 项目 B | 差异说明 |
|--------|--------|--------|----------|
| **文件位置** | src/pages/workflows/WorkflowList.tsx | src/pages/settings/WorkflowList.tsx | 路由组织不同 |
| **UI 框架** | shadcn/ui | 自研组件 + shadcn 混合 | 项目 B 混合使用 |
| **展示形式** | 表格列表 | 卡片列表 | **布局差异大** |
| **工作流状态** | ACTIVE/INACTIVE/DRAFT/ARCHIVED | active/inactive/draft | 项目 A 多 ARCHIVED |
| **统计信息** | 总数/活跃数/执行次数/平均成功率 | 总数/活跃数/草稿数/总执行次数 | 相似 |
| **操作功能** | 编辑/暂停/启用/删除/查看日志 | 编辑/查看日志/启用禁用/删除 | 相似 |
| **API 调用** | Mock data | useWorkflows, useWorkflowObjects, useDeleteWorkflow, useToggleWorkflowStatus | **项目 B API 更规范** |
| **触发器类型** | 未详细展示 | record_created/record_updated/field_changed/stage_changed/scheduled/no_activity/date_reached/manual | **项目 B 触发器更详细** |

#### 关键代码差异

**项目 A - 表格列**:
```typescript
const columns: ColumnDef<WorkflowItem>[] = [
  { accessorKey: 'name', header: '工作流名称', /* ... */ },
  { accessorKey: 'objectType', header: '对象类型', /* ... */ },
  { accessorKey: 'stepsCount', header: '步骤数', /* ... */ },
  { accessorKey: 'status', header: '状态', /* ... */ },
  { accessorKey: 'executions', header: '执行次数', /* ... */ },
  { accessorKey: 'successRate', header: '成功率', /* ... */ },
  { accessorKey: 'lastRun', header: '最近执行', /* ... */ },
  { id: 'actions', header: '操作', /* ... */ },
]
```

**项目 B - 卡片展示**:
```typescript
const WorkflowCard: React.FC<{
  workflow: Workflow;
  objectName: string;
  onEdit: () => void;
  onViewLogs: () => void;
  onToggleStatus: () => void;
  onDelete: () => void;
}> = ({ workflow, objectName, onEdit, onViewLogs, onToggleStatus, onDelete }) => {
  const triggerLabel = triggerTypeOptions.find(opt => opt.value === workflow.trigger.type)?.label || workflow.trigger.type;
  const successRate = workflow.runCount > 0 ? Math.round((workflow.successCount / workflow.runCount) * 100) : 0;
  // ...卡片渲染
}
```

---

## 功能差异汇总

### 项目 A 独有功能

| 模块 | 功能 | 说明 |
|------|------|------|
| **客户管理** | 批量发送邮件 | 选中客户后群发邮件 |
| **客户管理** | 数据导出 (Excel/CSV/JSON) | 完整的导出功能 |
| **客户管理** | 行内编辑 | 直接在详情页编辑字段 |
| **商机管理** | 漏斗视图 | 可视化销售漏斗 |
| **商机管理** | 阶段推进 API | 完整的阶段流转逻辑 |
| **商机管理** | 成交/失败确认 | 带原因记录的关闭逻辑 |
| **商机管理** | 加权金额统计 | 概率加权计算 |
| **权限管理** | 权限矩阵 | 完整的 RBAC 权限配置 |
| **权限管理** | 数据范围 | 5 级数据可见范围 |
| **权限管理** | 字段级权限 | 可见性/可编辑性控制 |
| **自定义字段** | 字段类型说明 | 内置类型说明卡片 |
| **工作流** | 独立工作流模块 | workflows 路由下完整页面 |
| **报表** | 12 个报表页面 | 完整的报表系统 |
| **AI 功能** | 15 个 AI 页面 | AIAnalytics/AIAssistant/AIConfig 等 |
| **仪表盘** | 可编辑仪表盘 | DashboardEditor/DashletPanel |
| **设置** | 24 个设置页面 | 完整的系统配置 |

### 项目 B 独有功能

| 模块 | 功能 | 说明 |
|------|------|------|
| **客户管理** | AI 客户洞察 | CustomerSummaryAI/RelationshipChangeAI/SmartSuggestionsAI/RiskAlertAI |
| **客户管理** | 客户等级 (A/B/C/D) | 客户分级管理 |
| **客户管理** | 行业/公司规模 | 更详细的客户画像 |
| **商机管理** | Kanban 看板 | 内置看板组件 |
| **商机管理** | 触发器类型 | 8 种工作流触发器 |
| **报价管理** | 转合同功能 | 报价单转合同 |
| **自定义字段** | 按对象分组 | 清晰的字段组织 |
| **自定义字段** | 拖拽排序 | 字段顺序可拖拽 |
| **自定义字段** | 批量删除 | 多选批量操作 |
| **自定义字段** | 系统字段保护 | isSystem 标识防止误删 |
| **自定义字段** | 字段统计 | 每个对象的字段数量统计 |
| **工作流** | 触发器配置 | 详细的触发条件 |
| **呼叫中心** | 外呼任务 | callcenter 模块 |
| **知识库** | 知识搜索 | knowledge 模块 |
| **价目表** | 价格本管理 | pricebooks 模块 |
| **工作台** | 智能工作台 | workbench 组件集合 |

---

## 需要同步的功能清单

### 高优先级 (建议从项目 A 同步到项目 B)

| 功能 | 源项目 | 目标项目 | 工作量评估 | 说明 |
|------|--------|----------|------------|------|
| 权限管理系统 | A | B | 大 (3-5 天) | 完整的 RBAC 实现 |
| 数据导出功能 | A | B | 中 (1-2 天) | Excel/CSV/JSON导出 |
| 销售漏斗视图 | A | B | 中 (1-2 天) | 可视化漏斗 |
| 行内编辑 | A | B | 中 (1 天) | 详情页直接编辑 |
| 阶段推进 API | A | B | 小 (0.5 天) | 商机阶段流转 |

### 中优先级 (建议从项目 B 同步到项目 A)

| 功能 | 源项目 | 目标项目 | 工作量评估 | 说明 |
|------|--------|----------|------------|------|
| AI 客户洞察 | B | A | 大 (5-7 天) | AI 驱动的客户分析 |
| 自定义字段分组 | B | A | 中 (1-2 天) | 按对象分组展示 |
| 拖拽排序 | B | A | 中 (1 天) | 字段顺序拖拽 |
| 批量删除 | B | A | 小 (0.5 天) | 多选批量操作 |
| 系统字段保护 | B | A | 小 (0.5 天) | isSystem 标识 |
| 转合同功能 | B | A | 中 (1 天) | 报价单转合同 |

### 低优先级 (可选同步)

| 功能 | 源项目 | 目标项目 | 工作量评估 | 说明 |
|------|--------|----------|------------|------|
| 客户等级系统 | B | A | 小 (0.5 天) | A/B/C/D分级 |
| 行业/公司规模 | B | A | 小 (0.5 天) | 客户画像字段 |
| 看板视图 | B | A | 中 (1 天) | 商机看板 (项目 A 已有外部页面) |
| 触发器类型 | B | A | 小 (0.5 天) | 工作流触发器枚举 |
| 字段统计 | B | A | 小 (0.5 天) | 字段数量展示 |

---

## 架构建议

### 推荐采用项目 A 的架构

1. **API 层**: TanStack Query (React Query) 提供更好的缓存和状态管理
2. **UI 组件**: shadcn/ui + Tailwind 更现代化，可定制性更强
3. **表格组件**: @tanstack/react-table 更灵活，支持虚拟滚动
4. **类型安全**: 完整的 TypeScript 类型定义

### 推荐采用项目 B 的设计

1. **AI 功能集成**: AI 客户洞察是有价值的差异化功能
2. **字段管理**: 按对象分组 + 拖拽排序用户体验更好
3. **工作流触发器**: 详细的触发器类型支持更复杂的自动化
4. **系统保护**: isSystem 标识防止误删核心配置

---

## 总结

### 项目 A 优势
- ✅ 功能完整性高 (权限/导出/漏斗视图)
- ✅ API 集成完整 (TanStack Query)
- ✅ 现代化 UI (shadcn/ui)
- ✅ 类型安全 (TypeScript)
- ✅ 批量操作完善

### 项目 A 劣势
- ❌ 缺少 AI 功能
- ❌ 自定义字段管理体验一般
- ❌ 缺少系统字段保护

### 项目 B 优势
- ✅ AI 客户洞察功能
- ✅ 自定义字段管理体验好
- ✅ 工作流触发器详细
- ✅ 系统保护机制

### 项目 B 劣势
- ❌ 权限管理未实现
- ❌ 部分功能用 Mock 数据
- ❌ UI 框架混用 (Ant Design + shadcn)
- ❌ 缺少数据导出功能

### 最终建议

**推荐以项目 A 为基础，同步项目 B 的 AI 功能和字段管理体验。**

具体步骤:
1. 将项目 B 的 AI 组件集成到项目 A 的 CustomerDetail
2. 改进项目 A 的 CustomFields 页面，采用项目 B 的分组 + 拖拽设计
3. 补充项目 B 缺失的权限管理功能
4. 统一 UI 框架为 shadcn/ui
5. 补充工作流触发器类型定义

---

*报告生成完成*
